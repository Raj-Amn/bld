ways to write the array in java and cpp -->both

what is byte addressable

memory consumption byte ke hisaab se
(case == 0)
calculate the index memory location - >arr+ index*(size of data type)
arr (is the value of the first memory location suppose ;500)

(case == 1)
calculate the index memory location -> arr+(index-1)*(size of data type)

why indexing starts with 0

questions--
sizeof(element)  -->shows how much memeory some element is taking
find the size of element
find the size of array
sizeof(arr)
find the number of elements in the array
find minimum element in the array
find maximum element in the array
(max element and min element concept in cpp and java)

--java
int max = Integer.MIN_VALUE;
int min = Integer.MAX_VALUE;

--cpp
int max = INT_MIN;
int min = INT_MAX;

